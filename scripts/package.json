{
  "name": "waterval-generate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node --env-file-if-exists=.env generate-questions.mjs",
    "extract": "node --env-file-if-exists=.env extract-onthoudmap.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.16",
    "@anthropic-ai/sdk": "^0.65.0",
    "@google/genai": "^1.5.0",
    "@supabase/supabase-js": "^2.45.4"
  }
}
